import { Button, TestButton } from "ui";

const App = () => {
  return (
    <div>
      <TestButton />
      <Button variant="destructive">Destructive</Button>
    </div>
  );
};

export default App;
